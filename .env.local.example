# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Better Auth 
# Configure each of these as env variables for convex
# npx convex env set BETTER_AUTH_URL=http://localhost:3000
# Generate a secret with the command: pnpm run auth:secret
# npx convex env set BETTER_AUTH_SECRET=

# OAuth2 providers, optional, update as needed
# Convex needs those environment variables locally and remotely
# npx convex env set GITHUB_CLIENT_ID=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NOTE:
# In your OAuth2 apps, set callback/redirect URIs to`http://localhost:3000/api/auth/callback/<provider>`
# e.g. http://localhost:3000/api/auth/callback/github